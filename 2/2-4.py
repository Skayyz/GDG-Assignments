# 4 | Create a program that prompts for an input string and displays output that shows the input string and the number of characters the string contains.

User_name = str(input("What is your name ? ")) # Take the name of the user

print(f"{User_name} haas {len(User_name)} characters") # Print the number of characters of the name
